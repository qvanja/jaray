import { KhmerCurrencyPipe } from './khmer-currency-pipe';

describe('KhmerCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new KhmerCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
